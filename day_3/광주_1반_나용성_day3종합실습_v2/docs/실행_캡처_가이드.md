# 실행 & 캡처 가이드 (직접 실행용)

터미널에서 아래 순서대로 실행하고, 각 단계마다 **화면을 캡처**해서
`docs/` 폴더에 지정된 파일명으로 저장하면 됩니다.

- macOS 화면 캡처: **⌘⇧4** (영역 선택) → 데스크톱에 저장됨 → 아래 규칙대로 이름 변경 후 `docs/`로 이동
- psql에서 **SQL을 붙여넣으면 입력한 SQL과 결과가 한 화면에 같이 표시**되므로, 그 상태로 캡처하면 "SQL문 + 결과 화면"이 한 장에 담깁니다.

---

## 📁 파일명 규칙 (총 15장)

| 파일명 | 캡처 내용 | 리포트 섹션 |
|--------|-----------|-------------|
| `00_conn.png` | psql 접속 + `\conninfo`, `\dn`, `\dt` | 3. 접속 결과 화면 |
| `01_ddl.png`  | `CREATE TABLE` ×5 + 테이블 목록 | 4. DDL |
| `02_dml.png`  | `INSERT` 결과 + 건수 확인 | 5. DML |
| `03_q01.png`  | Q1 SQL + 결과 | 6. 기초 Q1 |
| `04_q02.png`  | Q2 SQL + 결과 | 6. 기초 Q2 |
| `05_q03.png`  | Q3 SQL + 결과 | 6. 기초 Q3 |
| `06_q04.png`  | Q4 SQL + 결과 | 6. 기초 Q4 |
| `07_q05.png`  | Q5 SQL + 결과 | 7. 함수 Q5 (COALESCE) |
| `08_q06.png`  | Q6 SQL + 결과 | 7. 함수 Q6 (CASE WHEN) |
| `09_q07.png`  | Q7 SQL + 결과 | 7. 함수 Q7 (날짜) |
| `10_q08.png`  | Q8 SQL + 결과 | 7. 함수 Q8 (근속연수) |
| `11_q09.png`  | Q9 SQL + 결과 | 8. JOIN Q9 |
| `12_q10.png`  | Q10 SQL + 결과 | 8. JOIN Q10 |
| `13_q11.png`  | Q11 SQL + 결과 | 8. JOIN Q11 |
| `14_q12.png`  | Q12 SQL + 결과 | 8. JOIN Q12 |

> 캡처를 줄이고 싶으면 Q1~Q4 / Q5~Q8 / Q9~Q12를 묶어서 3장으로 해도 되지만,
> 과제가 "문항별 SQL·결과 화면 필수(누락 시 감점)"라 **문항별 1장을 권장**합니다.

---

## 0단계. 사전 준비 (매 터미널 세션마다 1회)

```bash
export PATH="/opt/homebrew/opt/postgresql@17/bin:$PATH"
cd "/Users/nys/workspace/data-project/day_3/광주_1반_나용성_day3종합실습_v2"
```

---

## A단계. DB → 스키마 → DDL → DML 실행

```bash
# 1) 데이터베이스 생성 (postgres DB에 접속해서 실행)
psql -p 5433 postgres   -f sql/01_create_database.sql

# 2) 스키마 생성
psql -p 5433 university -f sql/02_create_schema.sql

# 3) DDL — 테이블 생성   👉 실행 후 화면 캡처 → 01_ddl.png
psql -p 5433 university -f sql/03_ddl_create_table.sql

# 4) DML — 데이터 입력   👉 실행 후 화면 캡처 → 02_dml.png
psql -p 5433 university -f sql/04_dml_insert.sql
```

- `01_ddl.png` : `CREATE TABLE` 5번 + 테이블 목록(`\dt`)이 보이는 화면
- `02_dml.png` : `INSERT 0 10 / 12 / 15 / 12 / 26` + 건수 확인 표가 보이는 화면

---

## B단계. 접속 확인 → `00_conn.png`

```bash
psql -p 5433 university
```

접속되면 프롬프트가 `university=#` 로 바뀝니다. 아래 3개를 차례로 입력:

```
\conninfo
\dn
\dt academy.*
```

👉 세 명령의 출력이 모두 보이는 상태로 캡처 → **`00_conn.png`**

---

## C단계. 문항별 조회 → `03_q01.png` ~ `14_q12.png`

**B단계에서 접속한 psql 창을 그대로 유지**한 채, 먼저 스키마 경로를 지정하고
아래 SQL 블록을 **하나씩 붙여넣기 → Enter → 캡처**를 반복합니다.

```sql
SET search_path TO academy, public;
```

### Q1 → `03_q01.png`  (공과대학 학과, 학과명 오름차순)
```sql
SELECT dept_id, dept_name, college, office_phone
FROM department
WHERE college = '공과대학'
ORDER BY dept_name ASC;
```

### Q2 → `04_q02.png`  (재학생, 입학일 최신순)
```sql
SELECT student_id, student_name, dept_id, admission_date, status
FROM student
WHERE status = '재학'
ORDER BY admission_date DESC, student_id ASC;
```

### Q3 → `05_q03.png`  (정원 40 초과 강의, 정원 내림차순)
```sql
SELECT course_code, course_name, credits, capacity, semester
FROM course
WHERE capacity > 40
ORDER BY capacity DESC;
```

### Q4 → `06_q04.png`  (2015년 이후 임용 교수, 임용일 오름차순)
```sql
SELECT prof_id, prof_name, position, hire_date
FROM professor
WHERE hire_date >= '2015-01-01'
ORDER BY hire_date ASC;
```

### Q5 → `07_q05.png`  (COALESCE — 미등록 항목 대체 표시)
```sql
SELECT student_name,
       COALESCE(phone, '(연락처 미등록)') AS phone,
       COALESCE(email, '(이메일 미등록)') AS email
FROM student
ORDER BY student_id;
```

### Q6 → `08_q06.png`  (CASE WHEN — 성적 등급 분류)
```sql
SELECT s.student_name, c.course_name, e.grade,
       CASE
           WHEN e.grade IN ('A+','A')          THEN '우수'
           WHEN e.grade IN ('B+','B')          THEN '양호'
           WHEN e.grade IN ('C+','C','D+','D') THEN '미흡'
           WHEN e.grade = 'F'                  THEN '낙제'
           ELSE '진행중'
       END AS grade_group
FROM enrollment e
JOIN student s ON s.student_id = e.student_id
JOIN course  c ON c.course_id  = e.course_id
ORDER BY e.grade NULLS LAST, s.student_name;
```

### Q7 → `09_q07.png`  (날짜 함수 — 만 나이 / 재학 개월 수)
```sql
SELECT student_name, birth_date,
       DATE_PART('year', AGE(CURRENT_DATE, birth_date))::INT AS age,
       admission_date,
       (DATE_PART('year',  AGE(CURRENT_DATE, admission_date)) * 12
      + DATE_PART('month', AGE(CURRENT_DATE, admission_date)))::INT AS months_enrolled
FROM student
WHERE birth_date IS NOT NULL
ORDER BY age DESC;
```

### Q8 → `10_q08.png`  (CASE WHEN + 날짜 — 교수 근속연수 구분)
```sql
SELECT prof_name, position, hire_date,
       DATE_PART('year', AGE(CURRENT_DATE, hire_date))::INT AS years_of_service,
       CASE
           WHEN DATE_PART('year', AGE(CURRENT_DATE, hire_date)) >= 15 THEN '시니어'
           WHEN DATE_PART('year', AGE(CURRENT_DATE, hire_date)) >= 8  THEN '미드'
           ELSE '주니어'
       END AS seniority
FROM professor
ORDER BY years_of_service DESC;
```

### Q9 → `11_q09.png`  (수강신청 5개 테이블 JOIN)
```sql
SELECT s.student_name, d.dept_name, c.course_code, c.course_name,
       COALESCE(p.prof_name, '(미배정)') AS professor,
       c.credits, COALESCE(e.grade, '진행중') AS grade
FROM enrollment e
JOIN student    s ON s.student_id = e.student_id
JOIN department d ON d.dept_id    = s.dept_id
JOIN course     c ON c.course_id  = e.course_id
LEFT JOIN professor p ON p.prof_id = c.prof_id
ORDER BY d.dept_name, s.student_name, c.course_code;
```

### Q10 → `12_q10.png`  (강의별 수강 인원 + 신청률)
```sql
SELECT c.course_code, c.course_name, c.capacity,
       COUNT(e.enroll_id) AS enrolled,
       ROUND(COUNT(e.enroll_id) * 100.0 / c.capacity, 1) AS fill_rate_pct
FROM course c
LEFT JOIN enrollment e ON e.course_id = c.course_id
GROUP BY c.course_id, c.course_code, c.course_name, c.capacity
ORDER BY enrolled DESC, c.course_code;
```

### Q11 → `13_q11.png`  (학생별 신청/취득학점/진행중)
```sql
SELECT s.student_name,
       COUNT(e.enroll_id) AS total_courses,
       SUM(CASE WHEN e.grade IS NOT NULL THEN c.credits ELSE 0 END) AS earned_credits,
       SUM(CASE WHEN e.grade IS NULL     THEN 1 ELSE 0 END)         AS in_progress
FROM student s
JOIN enrollment e ON e.student_id = s.student_id
JOIN course     c ON c.course_id  = e.course_id
GROUP BY s.student_id, s.student_name
ORDER BY earned_credits DESC, total_courses DESC;
```

### Q12 → `14_q12.png`  (학과별 강의 수 / 학생 수)
```sql
SELECT d.dept_name, d.college,
       (SELECT COUNT(*) FROM course  c WHERE c.dept_id = d.dept_id) AS course_count,
       (SELECT COUNT(*) FROM student s WHERE s.dept_id = d.dept_id) AS student_count
FROM department d
ORDER BY student_count DESC, d.dept_name;
```

끝나면 psql 종료:
```
\q
```

---

## ✅ 체크리스트

- [ ] `00_conn.png` (접속)
- [ ] `01_ddl.png` (DDL)  · `02_dml.png` (DML)
- [ ] `03_q01.png` ~ `14_q12.png` (문항 12개)

> 참고: 각 조회 SQL은 `sql/05_query_basic.sql`, `sql/06_query_functions.sql`,
> `sql/07_query_join.sql` 에도 동일하게 들어 있습니다. (파일 실행 결과와 위 SQL 결과는 동일)
