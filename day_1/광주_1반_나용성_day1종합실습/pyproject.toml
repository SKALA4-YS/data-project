[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
# E/W: pycodestyle, F: pyflakes, I: isort(import 정렬)
select = ["E", "F", "I", "W"]

[tool.pytest.ini_options]
# tests/ 에서 프로젝트 루트의 모듈(schemas 등)을 import 할 수 있게 함
pythonpath = ["."]
